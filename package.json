{
  "name": "teste-cap-railway",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/postgres": "^1.11.1",
    "@sap/cds": "^8",
    "@sap/xssec": "^4.2.8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.6",
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8.7.1"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "auth": "mocked",
      "db": "pg",
      "kinds": {
        "[pg]": {
          "kind": "postgres",
          "credentials": {
            "host": "localhost", "port": 5432,
            "user": "postgres",
            "password": "postgres",
            "database": "postgres"
          }
        }
      }
    }
  }
}
